import {
  EMPLOYEE_UPDATE
} from "../actions/index";

const INITIAL_STATE = {

};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
